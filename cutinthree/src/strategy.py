"""
Description : Strategy purposed
of cutting the array in block
of three elements.
Author : Yohan GOUZERH
Creation : 05 October 2018
"""
class CutInThree():
    """
        Recursively :

        - Take an array, sum the
        same signs elements.
        - Than, take this array, and try
        to constitue block by assembling
        three elements : one + and the two -
        of each side.
        - If the block is worst, throw it
        - If it is the end, take it and return it
    """
    def execute(self, sample):
        """
            Take a sample,
            execute the algorithm and return
            the sample
        """
        return sample
